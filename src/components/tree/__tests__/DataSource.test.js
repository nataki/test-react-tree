/**
 * Created by nataliia.akimova on 1/25/2017.
 */
import DataSource from '../DataSource';
const data = require('../../../data/data-minimal.json');

describe('findNode', () => {
    it('is defined', () => {
        expect(findNode).not.toBe(undefined);
    });
});
